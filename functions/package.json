{
  "name": "functions",
  "description": "Cloud Functions Gen2 — seul écrivain des champs financiers (invariant 3)",
  "type": "commonjs",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  },
  "private": true
}
